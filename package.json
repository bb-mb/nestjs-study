{
  "name": "nestjs",
  "version": "1.0.0",
  "description": "**# nestjs-study**",
  "main": "index.js",
  "scripts": {
    "docker-start": "docker compose up -d",
    "docker-restart": "docker compose down && docker compose build --no-cache && docker compose up -d"
  }
}
